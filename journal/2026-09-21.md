---
date: 2026-09-21_18:34:40+0200
agent: Codex (GPT-6)
current-repo: osg://repo/github.com/cristianvasquez/dot-triples
---

## 2026-09-21_18:34:40 - Strengthen the manifest types

The user requested precise types for naming helpers and asked whether GHC adds useful verification. [The manifest](../spec/manifest.hs) now separates opaque validated `Name` and `Token` values, IRI text, RDF node types, and legal quad positions. Naming conversions return `NamedNode` or `Literal`. Callback methods carry effects. Path-derived names and split heading components remain raw text because they can be empty or untrimmed.

JavaScript now exports `parseName` and `parseToken`, rejects non-string and malformed identifier inputs, and returns `null` when reverse URI decoding fails or produces an invalid identifier. `tokenToLiteral` rejects empty input. Predicate mappings reject non-NamedNode RDF terms. These changes tighten previously accepted inputs; valid name/token round trips still pass.

Added `pnpm test:spec` and a CI job. The compile checks exercise valid compositions and require invalid compositions to fail with type errors. Local verification passed: 135 JavaScript tests, 11 GHC contract checks with GHC 9.6.7, and `git diff --check`. The first compile-test run exposed indentation and diagnostic-matching errors in the new test harness; these were corrected before the passing run. The GitHub Actions job has not run locally.

GHC checks the Haskell model and sample compositions, not JavaScript conformance. Manifest function bodies remain stubs. JavaScript names and tokens remain strings, so their distinction is enforced only in the Haskell model; runtime checks enforce their shared validity rules. Existing unrelated defects listed in the manifest remain outside this change.
